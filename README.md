All .ipynb files can be executed in Google Colab once the required dataset files are uploaded and the corresponding file paths are updated.  
The dataset and its data dictionary are located in the `data` directory.

The EDA report is an initial exploratory version.  
The Modeling report is a more formal and polished version, written in LaTeX with a more standardized structure and presentation.
